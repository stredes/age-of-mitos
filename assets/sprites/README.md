# Sprite Asset Pipeline

Use `docs/asset_prompt_master.md` and `data/asset_manifest.json` for all generated PNG art.

Do not add random one-off sprites directly. Add a manifest entry first, generate with the shared prompt, then place the approved PNG in the manifest path.

Runtime procedural sprites remain as fallback until a full category has consistent coverage.
